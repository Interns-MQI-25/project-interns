<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Dashboard - Product Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/navbar') %>
    
    <main class="ml-64 p-6">
        <%- include('../partials/messages') %>
        
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900">Employee Dashboard</h1>
            <p class="text-gray-600">Welcome back, <%= user.full_name %>!</p>
        </div>
        
        <!-- Dashboard Cards -->
        <div class="flex flex-col items-center mt-16 space-y-8">
            <!-- Top Card -->
            <div class="flex justify-center">
                <a href="/employee/account" class="group">
                    <div class="bg-white hover:bg-blue-50 rounded-lg shadow hover:shadow-md transition-all duration-200 border border-gray-200 hover:border-blue-200 w-80">
                        <div class="p-8 text-center">
                            <div class="w-16 h-16 bg-blue-50 rounded-full flex items-center justify-center mx-auto mb-4">
                                <i class="fas fa-user text-xl text-blue-500"></i>
                            </div>
                            <h3 class="text-xl font-medium text-gray-800 mb-2">Employee Profile</h3>
                            <p class="text-sm text-gray-500">View personal information</p>
                        </div>
                    </div>
                </a>
            </div>
            
            <!-- Bottom Two Cards -->
            <div class="flex gap-8">
                <!-- View Stock Card -->
                <a href="/employee/stock" class="group">
                    <div class="bg-white hover:bg-green-50 rounded-lg shadow hover:shadow-md transition-all duration-200 border border-gray-200 hover:border-green-200 w-80">
                        <div class="p-8 text-center">
                            <div class="w-16 h-16 bg-green-50 rounded-full flex items-center justify-center mx-auto mb-4">
                                <i class="fas fa-boxes text-xl text-green-500"></i>
                            </div>
                            <h3 class="text-xl font-medium text-gray-800 mb-2">View Stock</h3>
                            <p class="text-sm text-gray-500">Browse available inventory</p>
                        </div>
                    </div>
                </a>

                <!-- View History Card -->
                <a href="/employee/records" class="group">
                    <div class="bg-white hover:bg-purple-50 rounded-lg shadow hover:shadow-md transition-all duration-200 border border-gray-200 hover:border-purple-200 w-80">
                        <div class="p-8 text-center">
                            <div class="w-16 h-16 bg-purple-50 rounded-full flex items-center justify-center mx-auto mb-4">
                                <i class="fas fa-history text-xl text-purple-500"></i>
                            </div>
                            <h3 class="text-xl font-medium text-gray-800 mb-2">View Records</h3>
                            <p class="text-sm text-gray-500">Check assignment records</p>
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </main>
</body>
</html>
