<!DOCTYPE html>
<html>
<head>
  <title>Request Product</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Request Product: <%= product.product_name %></h1>

  <% if (product) { %>
    <p><strong>Description:</strong> <%= product.description %></p>
    <p><strong>Available Quantity:</strong> <%= product.quantity %></p>

    <form action="/employee/requests/create/<%= product.product_id %>" method="POST">
      <div>
        <label for="quantity">Quantity:</label>
        <input type="number" id="quantity" name="quantity" required min="1" max="<%= product.quantity %>">
      </div>
      <div>
        <label for="purpose">Purpose:</label>
        <textarea id="purpose" name="purpose" required></textarea>
      </div>
      <%-- employee_id will be determined from the logged-in user --%>
      <button type="submit">Submit Request</button>
    </form>
  <% } else { %>
    <p>Product not found.</p>
  <% } %>

  <p><a href="/employee/products">Back to Product List</a></p>
</body>
</html>