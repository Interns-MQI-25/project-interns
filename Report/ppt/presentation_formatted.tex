<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI-Powered Educational Management System - Presentation</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"/>
  <style>
    html, body { margin: 0; padding: 0; }
    body {
      font-family: 'Inter', 'Segoe UI', Arial, sans-serif;
      background: linear-gradient(135deg, #141e30 0%, #243b55 100%);
      color: #1a202c;
      min-height: 100vh;
      overflow-x: hidden;
      transition: background 0.5s;
    }
    .dark-mode {
      background: linear-gradient(135deg,#090b1c 0%,#16182c 100%);
      color: #eaecef;
    }
    .container {
      width: 100vw;
      min-height: 100vh;
      display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      padding: 45px 0;
    }
    .slide {
      width: 980px; max-width: 98vw;
      background: rgba(255,255,255,0.12);
      backdrop-filter: blur(9px);
      border-radius: 26px;
      box-shadow: 0 6px 32px rgba(80,44,200,0.20);
      padding: 60px 60px 50px 60px;
      margin-bottom: 30px;
      opacity: 0; pointer-events:none;
      transform: translateY(80px) scale(0.96);
      transition: all 0.7s cubic-bezier(.63,.16,.41,.8);
      display: none;
      position: relative;
      z-index:1;
      border: 1px solid #ababab44;
    }
    .slide.active {
      display: block;
      opacity: 1; pointer-events:auto;
      transform: none;
      z-index: 2;
      animation: fadein .9s;
    }
    @keyframes fadein {
      from { opacity:.0; transform:scale(.92) translateY(80px)}
      to { opacity:1; transform:none }
    }
    h1 {
      font-size:3em;
      color: #7ed2ff;
      font-weight:900;
      border-bottom:2px solid #46aaf8bb;
      padding-bottom:13px;
      margin:20px 0 40px 0;
      letter-spacing:1px;
      /* glass effect text */
      text-shadow: 0 2px 25px #5dcafab2, 0 1px 0 #141e3099;
    }
    .subtitle {
      font-size:1.7em;
      color:#c3b0fb;
      margin:0 0 35px 0;
      font-weight:500;
      text-align:center;
    }
    .company-info, .authors, .team-info {
      background: rgba(210,245,255,0.13);
      border-radius:18px; box-shadow:0 2px 8px #6783bb55;
      margin:23px 0 10px 0; padding:16px 26px;
      font-size:1.16em;
      border-left:5px solid #6a85ea;
      font-weight:500;
    }
    .card {
      background: linear-gradient(120deg, #323587 0%, #7ed2ff 100%);
      color: #fff;
      font-weight:500;
      padding:18px 26px;
      border-radius:15px; box-shadow:0 2px 7px #3e54a2ad;
      margin:12px 0;
    }
    .features-grid, .tech-grid, .deployment-options {
      display:flex;gap:20px;flex-wrap:wrap;
      justify-content:center;
      margin:25px 0;
    }
    .feature-card, .tech-card, .deployment-card {
      flex:1 1 250px; 
      background: rgba(245,255,255,0.17);
      border-radius:16px; box-shadow: 0 1px 22px #6a85ea18;
      padding:25px 18px; text-align:center;
      border:2px solid #212f4440;
      transition: background .5s;
    }
    .feature-card {
      background: linear-gradient(135deg,#7ed2ff 25%,#6a85ea 100%);
      color: #212f44;
      font-weight:600;
    }
    h2, h3 {margin:20px 0 10px 0;}
    ul, li {font-size:1.25em; margin:0; padding:0;}
    ul {padding-left:2em;}
    li {
      margin:10px 0;
      padding-left: .5em;
      line-height:1.6;
      border-left: 4px solid #7ed2ff61;
      background:rgba(245,255,255,0.07);
      border-radius:5px;
    }
    .architecture-diagram {text-align:center;margin:40px 0;}
    .layer {
      font-weight:700;
      color:#fff; background:#232b4c;
      padding:17px;margin:13px 0;
      border-radius: 14px;
      font-size:1.2em;
      box-shadow:0 1px 10px #6783bb55;
    }
    .arrow {font-size:2.6em;color:#7ed2ff;margin:0;}
    .code-block {
      background: #151a2d;
      color: #d8efff;
      padding:17px 16px;
      font-family: "JetBrains Mono",monospace;
      border-radius:11px;
      font-size:1.1em;
      margin:13px 0;
      overflow-x:auto;
    }
    .slide-controls {position: fixed;
      bottom:38px; left:50%; transform:translateX(-50%);
      display: flex; gap:24px;z-index:20;}
    .nav-btn {background:#7ed2ff;color:#212f44;padding:13px 32px; border:none; border-radius:36px;
      font-size:1.2em; cursor:pointer;font-weight:700; letter-spacing:.5px;box-shadow:0 1px 7px #212f4455;}
    .nav-btn:hover {background:#2e9df7; color:#fff;}
    .nav-btn:disabled{background:#bdc7e3;color:#aaa;}
    .slide-counter {position:fixed;top:28px;right:42px;background:#212f44bb;padding:12px 28px;
      border-radius:22px;font-weight:800;color:#c3b0fb;box-shadow:0 2px 20px #262b390f;z-index:20;
      font-size:1.22em;}
    .utility-controls {position:fixed;bottom:38px;right:42px;display:flex;gap:17px;z-index:20;}
    .util-btn {background:#232b4c;color:#fff;border:none;padding:14px 15px;border-radius:38px;cursor:pointer; font-size:1.38em;}
    .util-btn:hover {background:#7ed2ff; color:#212f44;}
    .util-btn.active {background:#c3b0fb; color:#191d34;}
    .dark-toggle {position:fixed;top:28px;left:35px;z-index:30;}
    .dark-switch{background:#c3b0fb;color:#232b4c;border:none;padding:11px 15px;border-radius:25px;cursor:pointer;font-size:1.5em;}
    .dark-switch.active {background:#232b4c;color:#fff;}
    /* Mobile */
    @media (max-width:650px){
      .slide,.container{width:100vw;padding:6vw;}
      .features-grid,.tech-grid,.deployment-options{flex-direction:column;gap:10px;}
      .slide-controls,.utility-controls{bottom:13px;}
      .slide-counter{top:9px; right:14px;}
      .dark-toggle{top:6px;left:9px;}
    }
    /* SVG Logo style */
    .logo { width:70px; height:70px; position:absolute; top:10px; right:12px; opacity:.78;}
    /* Hide utility controls when printing */
    @media print {
      .utility-controls, .slide-controls, .slide-counter, .dark-toggle {display:none !important;}
      .slide { box-shadow:none !important;}
      body, .slide, .container {background: #fff !important;}
    }
  </style>
</head>
<body>
  <div class="dark-toggle">
    <button class="dark-switch" id="darkBtn" title="Toggle Dark Mode"><i class="fas fa-moon"></i></button>
  </div>
  
  <div class="container">
    <!-- Logo (optional SVG) -->
    <svg class="logo" viewBox="0 0 100 100">
      <circle cx="50" cy="50" r="41" fill="#87ceeb" opacity="0.6"/>
      <text x="50" y="58" text-anchor="middle" font-size="50" fill="#243b55" font-family="sans-serif">AI</text>
    </svg>
    <!-- Example Slide 1 -->
    <div class="slide active">
      <h1>AI-Powered Educational Management System</h1>
      <div class="subtitle">A Comprehensive Full-Stack Solution</div>
      <div class="company-info">
        <strong>Presented by:</strong> Your Team Name<br>
        <strong>Guide:</strong> Dr. Guide Name
      </div>
      <div class="authors">
        <strong>Team Members:</strong>
        <br>Member 1<br>Member 2<br>Member 3
      </div>
    </div>
    <!-- Example Slide 2 -->
    <div class="slide">
      <h1>Project Overview</h1>
      <div class="card">Mission: Revolutionizing education through intelligent tech and seamless experiences.</div>
      <ul>
        <li><i class="fas fa-users"></i> Multi-role dashboards: admin, teacher, student</li>
        <li><i class="fas fa-robot"></i> AI integration for personalized learning</li>
        <li><i class="fas fa-language"></i> Bilingual support and real-time translation</li>
        <li><i class="fas fa-bolt"></i> Scalable deployment (Docker, Cloud, JWT)</li>
      </ul>
    </div>
    <!-- Example Slide 3 -->
    <div class="slide">
      <h1>Tech Stack</h1>
      <div class="tech-grid">
        <div class="tech-card"><strong>Frontend:</strong> React.js, Tailwind CSS, Socket.io Client, i18next</div>
        <div class="tech-card"><strong>Backend:</strong> Node.js, Express.js, Socket.io, MongoDB</div>
        <div class="tech-card"><strong>AI/APIs:</strong> OpenAI, Google Translate, TensorFlow.js</div>
        <div class="tech-card"><strong>Deployment:</strong> Docker, Nginx, JWT Auth</div>
      </div>
    </div>
    <!-- Example Slide 4 -->
    <div class="slide">
      <h1>System Architecture</h1>
      <div class="architecture-diagram">
        <!-- Replace with img or SVG for system diagram -->
        <div class="layer">Frontend Layer (React.js)</div>
        <div class="arrow"><i class="fas fa-arrow-down"></i></div>
        <div class="layer">API Gateway (Express.js)</div>
        <div class="arrow"><i class="fas fa-arrow-down"></i></div>
        <div class="features-grid">
          <div class="feature-card"><strong>Auth</strong> <br> Secure login & JWT flow </div>
          <div class="feature-card"><strong>AI Service</strong> <br> AI-powered tutoring and analytics </div>
          <div class="feature-card"><strong>Real-time</strong> <br> Live class and dashboard updates </div>
        </div>
        <div class="arrow"><i class="fas fa-arrow-down"></i></div>
        <div class="layer">Database Layer (MongoDB)</div>
      </div>
    </div>
    <!-- More slides: copy your Beamer/PPT content into slide blocks below! -->
  </div>
  <div class="slide-counter">
    <span id="current-slide">1</span> / <span id="total-slides">4</span>
  </div>
  <div class="slide-controls">
    <button class="nav-btn" id="prev-btn" onclick="previousSlide()"><i class="fas fa-arrow-left"></i> Previous</button>
    <button class="nav-btn" id="next-btn" onclick="nextSlide()">Next <i class="fas fa-arrow-right"></i></button>
  </div>
  <div class="utility-controls">
    <button class="util-btn" onclick="window.print()" title="Export as PDF">
      <i class="fas fa-print"></i>
    </button>
    <button class="util-btn" onclick="downloadPPT()" title="Export as PPT">
      <i class="fas fa-file-powerpoint"></i>
    </button>
    <button class="util-btn" onclick="toggleFullScreen()" title="Full Screen">
      <i class="fas fa-expand"></i>
    </button>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/pptxgenjs/3.11.0/pptxgen.min.js"></script>
  <script>
    // Slide navigation logic
    let currentSlide = 0;
    const slides = document.querySelectorAll('.slide');
    const totalSlides = slides.length;
    document.getElementById('total-slides').textContent = totalSlides;
    function showSlide(n) {
      slides[currentSlide].classList.remove('active');
      currentSlide = (n + totalSlides) % totalSlides;
      slides[currentSlide].classList.add('active');
      document.getElementById('current-slide').textContent = currentSlide + 1;
      document.getElementById('prev-btn').disabled = currentSlide === 0;
      document.getElementById('next-btn').disabled = currentSlide === totalSlides - 1;
    }
    function nextSlide() { if (currentSlide < totalSlides - 1) showSlide(currentSlide + 1); }
    function previousSlide() { if (currentSlide > 0) showSlide(currentSlide - 1); }
    document.addEventListener('keydown', function(e) {
      if (e.key === 'ArrowRight' || e.key === ' ') nextSlide();
      else if (e.key === 'ArrowLeft') previousSlide();
    });
    function toggleFullScreen() {
      if (!document.fullscreenElement) document.documentElement.requestFullscreen();
      else document.exitFullscreen();
    }
    function downloadPPT() {
      let pptx = new PptxGenJS();
      slides.forEach(slide => {
        let sld = pptx.addSlide();
        sld.addText(slide.querySelector('h1')?.innerText||'',{x:0.5,y:0.2,fontSize:31,bold:true,color:'7ED2FF'});
        sld.addText(slide.querySelector('.subtitle')?.innerText||'',{x:0.5,y:0.8,fontSize:20});
        let items = slide.querySelectorAll('li');
        let lines = Array.from(items).map(el=>el.innerText);
        if(lines.length) sld.addText(lines.join('\n'),{x:0.9,y:1.3,fontSize:16});
      });
      pptx.writeFile('Presentation.pptx');
    }
    showSlide(0);
    document.getElementById('darkBtn').onclick = function() {
      document.body.classList.toggle('dark-mode');
      this.classList.toggle('active');
    }
  </script>
</body>
</html>
