<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Products - Product Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/navbar') %>
    
    <main class="ml-64 p-6">
        <%- include('../partials/messages') %>
        
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900">My Products</h1>
            <p class="text-gray-600">Complete history of all products assigned to you</p>
        </div>

        <!-- My Products Table -->
        <div class="bg-white shadow rounded-lg">
            <div class="px-4 py-5 sm:p-6">
                <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">
                    Product Assignment History (<%= myProducts.length %> items)
                </h3>
                
                <% if (myProducts.length === 0) { %>
                    <div class="text-center py-12">
                        <div class="text-gray-400">
                            <i class="fas fa-laptop text-4xl mb-4"></i>
                            <p class="text-lg">No product history found</p>
                            <p class="text-sm mt-2">Visit the stock page to request your first product</p>
                        </div>
                        <a href="/employee/stock" class="mt-4 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">
                            <i class="fas fa-boxes mr-2"></i>Browse Stock
                        </a>
                    </div>
                <% } else { %>
                    <div>
                        <table class="w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-2/5">
                                        Product Name
                                    </th>
                                    <th class="px-2 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/12">
                                        Asset Type
                                    </th>
                                    <th class="px-2 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/12">
                                        Model
                                    </th>
                                    <th class="px-2 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/12">
                                        Assigned Date
                                    </th>
                                    <th class="px-2 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/12">
                                        Assigned By
                                    </th>
                                    <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/6">
                                        Status
                                    </th>
                                </tr>
                            </thead>
                            <tbody class="bg-white divide-y divide-gray-200">
                                <% myProducts.forEach(product => { %>
                                    <tr>
                                        <td class="px-4 py-4">
                                            <div class="text-sm font-medium text-gray-900 break-words leading-tight"><%= product.product_name %></div>
                                            <% if (product.serial_number) { %>
                                                <div class="text-xs text-gray-400 mt-1">S/N: <%= product.serial_number %></div>
                                            <% } %>
                                        </td>
                                        <td class="px-2 py-4 whitespace-nowrap">
                                            <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
                                                <%= product.asset_type %>
                                            </span>
                                        </td>
                                        <td class="px-2 py-4 text-sm text-gray-500">
                                            <div class="break-words"><%= product.model_number || 'N/A' %></div>
                                        </td>
                                        <td class="px-2 py-4 whitespace-nowrap text-sm text-gray-500">
                                            <%= new Date(product.assigned_at).toLocaleDateString() %>
                                        </td>
                                        <td class="px-2 py-4 text-sm text-gray-500">
                                            <div class="break-words"><%= product.monitor_name %></div>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <% if (product.is_returned) { %>
                                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">
                                                    Returned
                                                </span>
                                            <% } else if (product.return_status === 'requested') { %>
                                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">
                                                    Return Requested
                                                </span>
                                            <% } else { %>
                                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                                                    Currently Using
                                                </span>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>